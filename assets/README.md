# Figures

Drop the paper figures here so the README renders them:

- `teaser.png` — the teaser (paper Fig. 1: semantic-slider reshaping results).
- `architecture.png` — the method overview (paper Fig. 5: training/inference pipeline).

PNG or JPG both work; keep them reasonably sized (< ~2 MB each) for a fast-loading README.
